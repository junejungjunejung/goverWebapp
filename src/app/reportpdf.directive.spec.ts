import { ReportpdfDirective } from './reportpdf.directive';

describe('ReportpdfDirective', () => {
  it('should create an instance', () => {
    const directive = new ReportpdfDirective();
    expect(directive).toBeTruthy();
  });
});
